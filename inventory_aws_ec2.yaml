---
#plugin configuration file
plugin: aws_ec2
inventory:
  - us-east-2
  
filters:
 tag:Name: dev*
 instance-state-name: running

#allows dosplayinh the instances by group based on tag
#eyed_groups:
 #- key: tags
  # prefix: "tag"